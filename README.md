# Gatemirror SSG

Static Site Generator tabanlı medya platformu.

## Klasör Yapısı

- `content/` - Ham içerik (Markdown dosyaları)
- `templates/` - HTML şablonları
- `public/` - SSG çıktısı (Cloudflare Pages burayı yayınlayacak)
- `bots/` - Python botları (creator, translator, builder)
